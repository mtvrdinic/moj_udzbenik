#include <bits/stdc++.h>
#include <iostream> 

using namespace std;


int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(NULL);

  char s[1000];

  for(int i = 0; i < 242383; i++){
  	cin.std::istream::getline (s, 1000);
  	
    
    //nova skola u zupaniji
    if(s[0] == ';' && s[1] == ';' && s[2] == ';' && s[3] == ';' && s[4] == ';'){
      cin.std::istream::getline (s, 1000);
      if(s[0] == ';' && s[1] == ';' && s[2] == ';' && s[3] == ';' && s[4] == ';'){
        cin.std::istream::getline (s, 1000);
        cout << s << endl;
      } 
    }
  }

  return 0;
}